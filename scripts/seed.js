// The curated bootstrap seed has been retired: data/jobs.json is the single
// source of truth in the repo (docs/index.html is the generated site). The
// seed scripts therefore regenerate data/jobs.json unchanged (no fake/
// search-URL rows to merge back in).
export const seedJobs = [];